# INVS Mobile MVP Fixed

Versión corregida del MVP INVS.

## Correr

```bash
npm install
npx expo start -c
```

Luego abrir Expo Go en el iPhone y escanear el QR.

Esta versión no usa expo-router. El punto de entrada real es `index.js`.
